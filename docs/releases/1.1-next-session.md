# Windows 1.1: next-session checklist

## Start here

- PR #452 is merged as `b664565a`, and follow-up #453 as `7772b570`. Daily `.25` is published from that main commit. Do not reopen or reapply either PR.
- Release sequence: Daily for existing testers first, then RC/beta, then stable. No broad stable release is authorized by this checklist.
- Use the current checkout with `F:\typewhisper\typewhisper-dev-tools\build-typewhisper-windows-dev.ps1 --run --winui <checkout>` for local app builds and launches.
- Preserve the user's normal profile. Use an isolated development profile for destructive or first-run tests. Release builds use the production profile; a separate directory alone does not isolate them.

## Already verified

- [x] Fresh wizard profile: plugin and model download, first dictation produces text.
- [x] New-profile dictation shortcut is Ctrl+Shift; existing shortcuts are preserved.
- [x] Microphone/hotkeys recover after standby and after USB microphone switching (Marco's hardware tests).
- [x] License activation and Apple account/license linking (Marco's tests).
- [x] Active-screen overlay follows the selected screen behavior (Marco's test).
- [x] Switch focus during dictation between two windows and between input fields, then return and paste into the original field (Marco confirmed successful tests; no repeat required).
- [x] .NET-bootstrap candidate packaging passes for x64 and ARM64. Package hashes and architecture-specific runtime prerequisites verified.
- [x] CLI installation, `--version`, `--help`, and removal from the shared-.NET candidate pass with isolated installation/PATH storage.
- [x] Raycast integration works (Marco confirmed successful testing; no repeat required).

## Daily acceptance and remaining checks

1. [x] PR #452 is merged with green final-head CI. Follow-up #453 final head `190a208f` passed all CI and review with no unresolved threads, then merged as `7772b570`.
2. [x] Tested x64 Setup without .NET 10 on Windows 11 Home 24H2, build `26100.9445`: missing-runtime detection, cancellation/retry, download, Microsoft .NET Runtime 10.0.12 installation and app startup passed. ARM64 runtime installation still requires ARM64 hardware.
3. [x] With .NET 10 present, Setup Repair and the extracted Velopack portable launcher both reached Welcome without another runtime installation.
4. [x] Installed bundled-runtime `.10` updated to published shared-runtime `.25` through Account & about in the clean x64 VM. The production feed offered `.25`; Download and restart handed off to the updater, installed missing .NET 10.0.12 after UAC, and automatically restarted the app. The installed version, saved dictionary word, and final up-to-date check were verified afterward. Setup-driven `.10` to `.22` migration also passed. The source candidate need not itself be published; the target must be discoverable through the feed. Active-recording updates and ARM64 execution are not covered by this idle x64 run.
5. [ ] Remaining display checks: primary-display mode, mixed DPI, all overlay layouts and live transcript positioning. Window/input-field switching already passed. Marco excluded monitor power cycling/disconnection and closing the target window from the requested acceptance scope; do not schedule those tests again or label them as passed. Marco reports no known dictation-cancellation problem and does not require an additional dedicated test; this is user feedback, not a newly executed cancellation test. A failed restore must still preserve text for review, never paste into an unrelated field.
6. [x] Local checks already covered the first-run wizard, tray-only startup with the completed profile, unchanged selected profile files, API status/capabilities/docs (HTTP 200), and CLI version/help/install/removal. The encrypted Premium session changed during account refresh and was excluded from byte-level preservation evidence. Raycast passed Marco's test. See the post-merge review section in the validation notes; no repeat is scheduled.
7. [x] Reviewed the Daily notes and published `.25` from main `7772b570` through explicit `publish_daily=true`, run `34494034213`. Both architecture jobs and publication passed. This is a Daily prerelease only; the remaining hardware checks above stay open.

## After the first Daily

- [ ] Gather feedback from existing Daily users and resolve reproducible blockers before RC.
- [ ] Validate ARM64 on real hardware. Current ARM64 evidence is cross-build/package validation, not hardware execution.
- [ ] Publish Stable/RC WinUI feeds only when those stages are ready. Keep `TypeWhisperDaily` package identity and architecture-specific WinUI feeds; do not mix legacy/plugin releases into updater results.
- [x] Audit the remaining portable plugin migration against the catalog: see [the 2026-09-10 audit](1.1-plugin-audit.md). Three of 39 user-facing plugins are published in v2; two more have portable build targets; 34 still have only Windows/WPF project targets. The 40th source project is an internal Parakeet dependency.
- [ ] Complete and validate the remaining portable migrations and v2 publication flow before claiming catalog parity. Preserve legacy plugin packages and the legacy feed.
- [ ] Calendar OAuth remains deferred: Microsoft/Google app registrations do not exist yet. Calendar sign-in should not be presented as available.

## References and known limits

- Follow-up PR: https://github.com/TypeWhisper/typewhisper-win/pull/453
- Published Daily: https://github.com/TypeWhisper/typewhisper-win/releases/tag/v1.1.0-daily.20260910.25
- Package provenance: `docs/releases/1.1-daily-validation.md`.
- VM-tested shared-.NET candidate: `1.1.0-daily.20260910.22`, run 34476111058, PR code head `17d043ad`, packaged merge commit `7100aca3`; x64 setup 42,715,337 bytes. Both architecture package jobs passed. See validation notes for exact evidence and hashes.
- Focus diagnostics: development profile `paste-diagnostics.log`. Logs contain stage/state markers, not dictated text. Focus readiness gates delivery; the five-second deadline only aborts an unreachable target and excludes transcription time.
- Clean-machine x64 setup and the full idle in-app update are verified in Proxmox VM 101 (`typewhisper-test`). The VM now has `.25`, .NET 10.0.12 and dictionary probe `inappprobe20260910`. Its setup wizard remains unfinished, and no transcription engine/model was installed for this update-only test.
