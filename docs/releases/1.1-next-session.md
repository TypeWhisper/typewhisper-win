# Windows 1.1: next-session checklist

## Start here

- PR #452 is merged as `b664565a`. Continue with follow-up PR #453 (`seofood/1.1-daily-review-fixes`); do not reopen or reapply #452.
- Release sequence: Daily for existing testers first, then RC/beta, then stable. No broad stable release is authorized by this checklist.
- Use the current checkout with `F:\typewhisper\typewhisper-dev-tools\build-typewhisper-windows-dev.ps1 --run --winui <checkout>` for local app builds and launches.
- Preserve the user's normal profile. Use an isolated development profile for destructive or first-run tests. Release builds use the production profile; a separate directory alone does not isolate them.

## Already verified

- [x] Fresh wizard profile: plugin and model download, first dictation produces text.
- [x] New-profile dictation shortcut is Ctrl+Shift; existing shortcuts are preserved.
- [x] Microphone/hotkeys recover after standby and after USB microphone switching (Marco's hardware tests).
- [x] License activation and Apple account/license linking (Marco's tests).
- [x] Active-screen overlay follows the selected screen behavior (Marco's test).
- [x] Switch focus during dictation, release shortcut, return and paste into the original field (Marco's final test after target input queue fix).
- [x] .NET-bootstrap candidate packaging passes for x64 and ARM64. Package hashes and architecture-specific runtime prerequisites verified.
- [x] CLI installation, `--version`, `--help`, and removal from the shared-.NET candidate pass with isolated installation/PATH storage.

## Before the next Daily

1. [x] PR #452 is merged with green final-head CI. Late review findings are fixed in #453; code head `17d043ad` passed CI and review. Recheck the final #453 head before merging.
2. [x] Tested x64 Setup without .NET 10 on Windows 11 Home 24H2, build `26100.9445`: missing-runtime detection, cancellation/retry, download, Microsoft .NET Runtime 10.0.12 installation and app startup passed. ARM64 runtime installation still requires ARM64 hardware.
3. [x] With .NET 10 present, Setup Repair and the extracted Velopack portable launcher both reached Welcome without another runtime installation.
4. [ ] Exercise an actual installed-app update using two compatible WinUI Daily releases, including the transition from bundled to shared .NET. Verify download, orderly shutdown, prerequisite handling and restart. Development/portable hosts cannot validate the installed updater. Setup-driven `.10` to `.22` migration passed in the clean VM, including runtime installation and dictionary preservation; the in-app flow remains open.
5. [ ] Finish multi-monitor checks: primary-display mode, mixed DPI, monitor disconnect, all overlay layouts and live transcript positioning. Retest original-field paste across different windows and different fields within one app, including a closed target and cancellation. A failed restore must preserve text for review, never paste into an unrelated field.
6. [ ] Confirm the wizard's first-run behavior, tray-only startup after setup, existing-profile preservation and no regression in Raycast/API/CLI integration.
7. [ ] Review `docs/releases/1.1-daily.md`, then publish a validated main build as a Daily only. The workflow's explicit `publish_daily` input controls publication; PR candidate builds do not publish releases.

## After the first Daily

- [ ] Gather feedback from existing Daily users and resolve reproducible blockers before RC.
- [ ] Validate ARM64 on real hardware. Current ARM64 evidence is cross-build/package validation, not hardware execution.
- [ ] Publish Stable/RC WinUI feeds only when those stages are ready. Keep `TypeWhisperDaily` package identity and architecture-specific WinUI feeds; do not mix legacy/plugin releases into updater results.
- [ ] Audit the remaining portable plugin migration against the catalog. Preserve legacy plugin packages and the legacy feed. Do not infer that all 36–40 plugins have been migrated from a successful Parakeet test.
- [ ] Calendar OAuth remains deferred: Microsoft/Google app registrations do not exist yet. Calendar sign-in should not be presented as available.

## References and known limits

- Follow-up PR: https://github.com/TypeWhisper/typewhisper-win/pull/453
- Package provenance: `docs/releases/1.1-daily-validation.md`.
- VM-tested shared-.NET candidate: `1.1.0-daily.20260910.22`, run 34476111058, PR code head `17d043ad`, packaged merge commit `7100aca3`; x64 setup 42,715,337 bytes. Both architecture package jobs passed. See validation notes for exact evidence and hashes.
- Focus diagnostics: development profile `paste-diagnostics.log`. Logs contain stage/state markers, not dictated text. Focus readiness gates delivery; the five-second deadline only aborts an unreachable target and excludes transcription time.
- Clean-machine x64 runtime installation is verified in Proxmox VM 101 (`typewhisper-test`). A live in-app update still needs two compatible published WinUI Daily releases; a setup-driven upgrade does not verify that flow.
