# Windows 1.1 development validation — 2026-09-09

The WinUI development build passed the checks below on Windows x64. This is development-build evidence, not installer, signing, Store, ARM64, or production-release certification.

## Fixes found during validation

- Restored the audio test project's reference to Presentation. The linked hotkey state now depends on `RecordingMode` from that assembly.
- Updated the portable audio test's host version from `1.1` to `1.1.0`, matching the plugin's minimum host version. `System.Version` treats the unspecified build component as older.
- Unified the WinUI API version used by status, capabilities, and the discovery file. Status already reported `1.2`; the other two still reported `1.1`.

## Automated results

| Suite | Passed |
| --- | ---: |
| Core | 556 |
| Presentation | 1,129 |
| CLI | 65 |
| Portable PluginSDK | 244 |
| Dictation Audio | 5 |
| PluginSystem API / CLI filters | 92 |
| Total | 2,091 |

The audio suite used synthesized English speech and deterministic PCM capture, with actual local Parakeet and Canary inference. Immediate capture preserved the first audio block and the word “Bananas”; a deliberately delayed capture lost that opening word. Published portable plugins returned the expected sentence and unloaded successfully.

The PluginSystem run was filtered to `HttpApi`, `ApiServerController`, `CliInstall`, and `CliSupport`; it was not a full legacy application test run. Local logs are under `artifacts/`, including `audio-release-retest.log`, `api-release-tests.log`, and the named suite release logs.

## Native UI and live API / CLI

Used the prescribed development build helper and an isolated profile named `release-11-20260909`. No personal license, account, sync configuration, or history was copied into it.

- Fresh Quick Launch showed Settings, History, and Dictionary pinned in that order, with Suggestions collapsed and the search field focused.
- Opened Setup through Quick Launch search. Checked Welcome, Permissions, Hotkey, AI & Engine, and the final test page. Continue was disabled without a ready model.
- Downloaded Canary 180M Flash through plugin settings, then activated it. Setup resumed at AI & Engine, recognized the ready model, enabled Continue, and saved completion.
- Created a Dictation Only workflow. The name followed the template, Advanced started collapsed, no LLM was required, and Ctrl+S saved it. Right-click exposed the workflow actions; Pin to Quick Launch persisted the pin.
- Enabled the HTTP API through Advanced. The bundled CLI discovered the explicit test profile and returned the selected Canary model.
- CLI file transcription, before and after restart, returned “Bananas are yellow. This is a test of local transcription.” from a synthesized WAV. No microphone recording or paid cloud request was used.
- Status, models, history, rules, profiles, dictation status, recorder status, dictionary terms/corrections, capabilities, `/docs`, and `/docs/` returned HTTP 200. Settings export through the CLI succeeded.
- Unsupported PATCH on models returned 405. A browser-origin request returned 403.
- After rebuild/restart, status, capabilities, and discovery agreed on API version `1.2`. The selected model was ready, setup completion survived, and the saved workflow and pin reappeared in Quick Launch.
- Restored the normal development profile through the same helper afterward. Quick Launch showed the original Dictionary/History/Settings pin order, Ctrl+Space shortcut, and ready Parakeet model with Ctrl+Shift dictation.

## Scope and remaining acceptance

The empty debug profile opened Quick Launch; this check entered Setup explicitly. It does not establish production installer first-run behavior. The final Setup text field received focus, but no new physical-microphone dictation was performed there. Workflow creation, persistence, and pinning were exercised in the UI; live cloud-LLM workflow execution was not.

Marco separately confirmed real dictation returning to and inserting into its original field, license activation, Apple sign-in/license linking, and cloud folder synchronization earlier in this session. Those confirmations are distinct from this automated run.

Calendar account connection remains intentionally parked pending Microsoft/Google OAuth registrations. Installer upgrade/uninstall, clean-machine prerequisites, ARM64, and live cloud-provider acceptance remain separate release checks.
