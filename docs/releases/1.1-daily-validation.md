# WinUI Daily validation: update channels and on-demand plugins

This document records successive validation runs. Earlier candidate measurements and pending statements describe those runs; the latest acceptance section below supersedes them.

## Build and scope

Validated the PR #452 candidate from GitHub Actions run 34461775686 (PR merge commit `43b98502d1d761014ed0acf268b15cbb7bbcf3e1`, version `1.1.0-daily.20260910.8`). Both x64 and ARM64 release packaging passed. This candidate has not been published as a Daily release.

## Package measurements

ARM64 setup: 222,263,045 bytes before, 159,675,920 bytes after (212.0 to 152.3 MiB, approximately 28% smaller). The previous comparison candidate is `1.1.0-daily.20260910.5`.

x64 setup: 230,632,336 bytes before (published Daily `.6`), 167,570,067 bytes after (219.95 to 159.81 MiB, 60.14 MiB / 27.3% smaller). Its candidate payload is 120.87 MiB compressed for the app/runtimes plus 35.32 MiB for the CLI. SHA-256 verification passed for the x64 candidate, setup and full update package.

The compressed candidate payload contains 114.94 MiB of app/runtime files and 33.73 MiB of CLI files, with no bundled plugin directory. Previous bundled plugins occupied 49.95 MiB compressed. Removing the WinForms dependency also reduced the app/runtime payload.

SHA-256 verification passed for the downloaded ARM64 candidate and setup. Hashing the decompressed files found 186 CLI files identical to app-root files: 86.15 MiB unpacked / 33.56 MiB compressed. These are potential packaging savings; the CLI currently needs a complete independent runtime when installed and must not simply lose these files.

## Runtime cleanup follow-up

Run 34463977594 validated version `1.1.0-daily.20260910.10`, PR merge commit `87f310ca1174ac376d71c7a3bf2f33bcddcb6ea3` (branch head `5a603307`). Both WinUI architecture packages and the portable logic suites passed. This candidate remains unpublished.

The x64 setup is now **79,553,777 bytes / 75.87 MiB**, down from the intermediate 159.81 MiB above (52.5% smaller) and the original 219.95 MiB. Its candidate ZIP is 72.35 MiB: 72.08 MiB of app/runtime payload and 0.20 MiB of CLI payload. SHA-256 verification passed for the downloaded candidate, setup and full update package. Candidate validation also passed locally after extraction.

The WinUI host references a single-target portable SDK built from the same shared SDK sources. This prevents restore from selecting the legacy WPF SDK target. The legacy SDK and plugin packages remain unchanged. WinUI now references the required Windows App SDK components rather than the umbrella package, removing unused host ONNX, DirectML and AI payloads. The tray bridge assembly is restored separately to avoid reintroducing its old umbrella dependency.

CLI publishing records identical runtime files in a SHA-256 manifest and removes their duplicate copies from the download. Installation reconstructs a complete independent CLI from verified app-root files. It does not depend on the app directory after installation. The CLI remains an optional install from Advanced; it has not been converted into a plugin. The final candidate passed a real install, `--version`, `--help` and removal smoke test with an isolated destination and injected PATH storage; the real user PATH was unchanged.

23 CLI installation tests, 245 portable SDK/host tests and 11 packaging checks passed. The local development helper built and started the app; a synthesized WAV transcribed successfully through the installed Parakeet plugin, returning the expected sentence. Plugin-private ONNX remains available for actual inference.

The full-solution CI also exposed an AnyCPU build without a native architecture. Follow-up `6788a923` defaults unspecified WinUI builds to win-x64 and respects ARM64 or an explicit RID. Property evaluation was verified for both defaults; its full-solution CI rerun must pass before merging.

## Fresh profile

An isolated development profile installed Parakeet through the live portable catalog, downloaded the TDT model, activated it and reached the wizard test field. Marco confirmed successful dictation using Ctrl+Shift+F9. The normal development profile was restored afterwards.

The walkthrough found a startup race: the wizard could retain its install button after the plugin index finished loading. The follow-up fix hides the installation section when the package becomes available. The local helper build passed after the fix.

## Update behavior

15 update-controller tests passed, including blocked restart during recording, explicit downgrade messaging, failed download, stale-offer invalidation and download-before-shutdown ordering. Six GitHub release-source tests, 31 plugin-store tests and seven package rejection checks also passed.

The follow-up also changes the default dictation shortcut to Ctrl+Shift in the startup fallback, settings and wizard, using one shared constant. Existing persisted shortcuts remain unchanged. All 17 hybrid-hotkey tests (including modifier-only chords) passed together with the 15 update-controller tests. The helper build/start passed. These follow-ups were made after the measured CI candidate; their next CI run must pass before publication.

A live installed-app update has not been completed: the published Daily predates the updater, and a subsequent compatible Daily containing the updater is needed. Release builds use the production profile even from a separate directory. Windows Sandbox is unavailable on this machine, so no isolated clean-machine installer acceptance is claimed. ARM64 native payload headers were verified; ARM64 hardware execution remains untested.

## Shared .NET runtime and installer bootstrap

Run 34467887595 passed for both architectures at branch head `4f1a8b03` (candidate merge commit `f87748a2a4ffef14fff4a4513befc6bfc1de16d6`, version `1.1.0-daily.20260910.13`). App and CLI now publish framework-dependent; Windows App SDK remains bundled. Velopack declares `net10-x64-runtime` or `net10-arm64-runtime` in the update package, enabling prerequisite detection and installation before setup or an update. The portable ZIP requires separately installed .NET 10.

Measured setup sizes: x64 **42,713,245 bytes / 40.73 MiB**; ARM64 **42,179,534 bytes / 40.23 MiB**. Downloaded candidate, setup and full update package hashes passed for both architectures. Both update-package manifests contain the expected architecture-specific runtime prerequisite. The extracted x64 candidate passed validation, and its CLI passed installation, `--version`, `--help` and removal with an isolated destination and no real PATH changes. The installed CLI now uses the system .NET runtime; the smoke harness's historical self-contained success label does not describe this package.

14 rejection checks passed, including bundled .NET detection and invalid shared-runtime configuration. The required development helper build/start passed. Full-solution CI for the previous native-architecture correction also passed. Actual prerequisite download/installation on a clean machine without .NET remains untested; this development machine already has .NET installed. No Daily was published by this validation.

## Post-merge review follow-up

PR #452 merged as `b664565a` with successful final-PR-head checks. Four late review findings were confirmed against the merged code: the Daily title contained a replacement character, shared CLI metadata validation admitted installer-reserved filenames, a ten-page release search reported a definitive channel miss, and window/element focus restoration used separate five-second budgets.

The follow-up corrects the title, rejects reserved/blank runtime entries, reports an incomplete release search as a failed check at the pagination limit, and passes one deadline through both focus-restoration phases. Review also identified two boundary cases: the installer now rejects reserved runtime names case-insensitively, and neither window activation nor element focus is requested after the shared deadline expires. Local validation passed 20 package rejection checks, all 1,205 presentation tests (including combined focus-budget, deadline-boundary and reserved-filename regressions), six release-source tests in Debug, and the required WinUI development helper build/start. The local Release plugin-test build could not complete because the developer machine lacks the ARM64 MSVC redistributable files required by the legacy WhisperCpp packaging target; Release validation remains a CI requirement.

An isolated development profile opened the setup wizard and displayed Ctrl+Shift as the default hotkey. Returning to the existing completed profile started the app without a visible window. Hash comparisons confirmed unchanged setup, dictation hotkey, dictionary, snippets, workflows and license files. The encrypted Premium session was rewritten during account refresh and is excluded from byte-level preservation evidence. Local API status, capabilities and documentation returned HTTP 200; the bundled development CLI returned its version and help.

The subsequent x64 VM checks below verified clean-machine setup, prerequisite cancellation/retry and portable startup with .NET present. Publication and the full idle in-app update were completed in the later run below. The VM tests did not audit registry/filesystem residue or establish profile isolation between installed and portable copies.

## Windows 11 VM installer acceptance

On 2026-09-10, candidate `1.1.0-daily.20260910.22` from Actions run `34476111058` (PR #453 head `17d043ad`, packaged merge commit `7100aca3`) was tested in a fresh x64 Windows 11 Home 24H2 VM, OS build `26100.9445`, with 4 GB RAM. The setup SHA-256 was `3ba273686dfcd903cca844128019f76496ebd6cdbb6f50ea3267e2ed9772181a`. The test ISO carried both the current shared-runtime package and the earlier bundled-runtime candidate `.10`.

Setup detected the missing .NET 10 x64 runtime. Cancelling closed setup and returned to Explorer; restarting setup displayed the prerequisite prompt again. This confirms the visible cancellation/retry flow, but does not claim a registry or filesystem residue audit. Accepting installation downloaded Microsoft .NET Runtime 10.0.12 (x64). After Marco confirmed Windows UAC, runtime installation completed, TypeWhisper installed, and the Welcome wizard opened.

With the runtime present, rerunning setup offered Repair without requesting the prerequisite again. Repair completed and automatically reopened the Welcome wizard. After closing the installed wizard, the actual Velopack portable ZIP was extracted to a separate VM desktop directory; its launcher also opened the Welcome wizard without a prerequisite prompt. These observations supersede the earlier pending clean-machine and portable startup checks above. They do not establish profile isolation between installed and portable copies.

After restoring the clean VM snapshot, the bundled-runtime candidate `.10` installed and reached Welcome without a shared-runtime prerequisite. Setup was skipped for this migration-only test, and dictionary word `migrationprobe20260910` was saved and visibly verified. Running `.22` Setup detected the missing shared runtime, installed .NET 10.0.12 after Marco confirmed UAC, and explicitly offered an update from `.10` to `.22`. Accepting Update completed the setup-driven upgrade. The app opened again and retained the dictionary word (1 of 1 words). The unfinished wizard reappeared: Skip setup does not persist completion, so this test does not establish preservation of a completed wizard state.

This setup-driven migration alone does not verify the in-app updater's download, shutdown and restart flow. The separate in-app run below exercises that flow. Neither VM run establishes transcription or ARM64 hardware acceptance.

## Published Daily and in-app update acceptance

PR #453 final head `190a208f` passed CI and review with no unresolved threads, then merged as `7772b57022df20c999474e59361bc1b0fb7f7c9f`. Explicit main workflow run [34494034213](https://github.com/TypeWhisper/typewhisper-win/actions/runs/34494034213) passed both architecture jobs and published [Daily 1.1.0-daily.20260910.25](https://github.com/TypeWhisper/typewhisper-win/releases/tag/v1.1.0-daily.20260910.25) as a prerelease. The app, test, packaging and workflow code is unchanged from the VM-tested code head `17d043ad`; intervening changes are documentation only.

The published x64 feed identifies `TypeWhisperDaily` version `.25`, a full update package of 40,115,457 bytes, and SHA-256 `EC4250085224E1930428CD594BD8D272425EDB9CE9694386FEF6DD9CBEAF8A56`. x64 and ARM64 use separate WinUI Daily feeds.

VM 101 was restored to its clean Windows 11 Home 24H2 snapshot without shared .NET. The bundled-runtime `.10` installer was installed from the test ISO. Account & about confirmed installed version `1.1.0-daily.20260910.10` and the Daily channel. Dictionary word `inappprobe20260910` was saved and visibly verified. An initial in-app check correctly identified published `.6` as a downgrade; it was not installed.

After `.25` was published, Check for updates offered it. Clicking Download and restart closed the app settings and opened the TypeWhisper Daily Update prerequisite dialog, explicitly identifying `.10` to `.25` and missing .NET 10 x64. Install Update launched Microsoft .NET Runtime 10.0.12 (x64); Marco confirmed UAC. The updater finished and TypeWhisper automatically reopened its unfinished Welcome wizard, without a manual app launch. The dictionary word remained present after restart (1 of 1 words). This uses the installed app's production GitHub feed and updater, not the target Setup.exe. The source `.10` candidate itself did not need to be published; only the target release needed to be discoverable through the feed.

After dismissing the unfinished wizard, Account & about showed installed version `1.1.0-daily.20260910.25`, retained the Daily channel, and a fresh Check for updates returned "You are up to date on this channel." The full idle x64 in-app update, including the bundled-to-shared .NET transition, passed. This does not claim an update during active recording, preservation of a completed wizard state, transcription, or ARM64 execution. The VM remains on `.25` with the dictionary probe and without an installed transcription engine/model.
