<UserControl x:Class="TypeWhisper.Plugin.LiveTranscript.LiveTranscriptSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Padding="8">
    <StackPanel>
        <StackPanel x:Name="FontSizePanel">
            <TextBlock Text="Font Size"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4" />
            <DockPanel Margin="0,0,0,12">
                <TextBlock DockPanel.Dock="Right"
                           Width="30"
                           TextAlignment="Right"
                           VerticalAlignment="Center"
                           x:Name="FontSizeLabel"
                           Text="16" />
                <Slider x:Name="FontSizeSlider"
                        Minimum="12"
                        Maximum="32"
                        IsSnapToTickEnabled="True"
                        TickFrequency="1"
                        Value="16"
                        ValueChanged="FontSizeSlider_ValueChanged" />
            </DockPanel>
        </StackPanel>

        <!-- Opacity -->
        <TextBlock Text="Window Opacity"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />
        <DockPanel Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Right"
                       Width="40"
                       TextAlignment="Right"
                       VerticalAlignment="Center"
                       x:Name="OpacityLabel"
                       Text="85%" />
            <Slider x:Name="OpacitySlider"
                    Minimum="0.3"
                    Maximum="1.0"
                    TickFrequency="0.05"
                    IsSnapToTickEnabled="True"
                    Value="0.85"
                    ValueChanged="OpacitySlider_ValueChanged" />
        </DockPanel>

        <StackPanel x:Name="AutoHidePanel">
            <TextBlock Text="Auto-hide Delay"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4" />
            <DockPanel Margin="0,0,0,12">
                <TextBlock DockPanel.Dock="Right"
                           Width="52"
                           TextAlignment="Right"
                           VerticalAlignment="Center"
                           x:Name="AutoHideLabel"
                           Text="3.0s" />
                <Slider x:Name="AutoHideSlider"
                        Minimum="0"
                        Maximum="10"
                        TickFrequency="0.25"
                        IsSnapToTickEnabled="True"
                        Value="3"
                        ValueChanged="AutoHideSlider_ValueChanged" />
            </DockPanel>
        </StackPanel>

        <!-- Position -->
        <TextBlock Text="Window Position"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />
        <Button Content="Reset position"
                HorizontalAlignment="Left"
                Padding="12,6"
                Click="ResetPositionButton_Click" />
    </StackPanel>
</UserControl>
