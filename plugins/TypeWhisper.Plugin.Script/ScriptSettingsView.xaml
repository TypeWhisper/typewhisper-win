<UserControl x:Class="TypeWhisper.Plugin.Script.ScriptSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="0,4">
        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="+ Add Script" Click="OnAddScript" Padding="12,6" Margin="0,0,8,0" />
            <Button Content="Remove" Click="OnRemoveScript" Padding="12,6" Margin="0,0,8,0" />
            <Button Content="Move Up" Click="OnMoveUp" Padding="12,6" Margin="0,0,8,0" />
            <Button Content="Move Down" Click="OnMoveDown" Padding="12,6" Margin="0,0,0,0" />
        </StackPanel>

        <!-- Script List -->
        <ListBox x:Name="ScriptList" MinHeight="120" MaxHeight="250"
                 SelectionChanged="OnSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <CheckBox IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                  Checked="OnToggleChanged" Unchecked="OnToggleChanged"
                                  Tag="{Binding Id}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                   VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock VerticalAlignment="Center" Foreground="Gray" FontSize="12">
                            <Run Text="[" /><Run Text="{Binding Shell, Mode=OneWay}" /><Run Text="]" />
                            <Run Text=" " />
                            <Run Text="{Binding Command, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Empty State -->
        <TextBlock x:Name="EmptyState" Text="No scripts configured."
                   Foreground="Gray" FontStyle="Italic" Margin="0,4,0,12" />

        <!-- Edit Panel -->
        <Border x:Name="EditPanel" BorderBrush="#E0E0E0" BorderThickness="1"
                CornerRadius="4" Padding="12,8" Margin="0,12,0,0" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,0,6" />
                <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,6"
                         TextChanged="OnEditFieldChanged" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Command:" VerticalAlignment="Center" Margin="0,0,0,6" />
                <TextBox x:Name="CommandBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,6"
                         TextChanged="OnEditFieldChanged" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Shell:" VerticalAlignment="Center" Margin="0,0,0,6" />
                <ComboBox x:Name="ShellCombo" Grid.Row="2" Grid.Column="1" Margin="0,0,0,6"
                          SelectedValuePath="Content" SelectionChanged="OnEditFieldChanged">
                    <ComboBoxItem Content="cmd" />
                    <ComboBoxItem Content="powershell" />
                </ComboBox>

                <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Gray" FontSize="11"
                           TextWrapping="Wrap"
                           Text="The transcribed text is passed via stdin. The script's stdout becomes the new text. Environment variables: TYPEWHISPER_APP_NAME, TYPEWHISPER_LANGUAGE, TYPEWHISPER_PROFILE" />

                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                            Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="CancelButton" Content="Cancel" Click="OnCancelEdit"
                            IsEnabled="False" Padding="12,6" Margin="0,0,8,0" />
                    <Button x:Name="SaveButton" Content="Save" Click="OnSaveEdit"
                            IsEnabled="False" Padding="12,6" />
                </StackPanel>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
