# Plugin settings screenshots

Real screenshots captured on 2026-09-19 from the combined Windows development app used for manual acceptance of PRs #478 through #485. The host checkout was `seofood/plugin-manual-acceptance` at `f454a740`, with the independently installed portable plugins. Shared Save controls and provider icons from the related host PRs are therefore visible together.

These images document the UI state, not separate builds of each PR or fresh provider API tests. Soniox captures show the manually accepted installed 1.3.0 package; subsequent source-only shutdown, subtitle alignment and packaging corrections are covered by the tests in #485. Cloudflare has no account configured. The dictation screenshot documents direct-insertion settings, not an insertion result in an external editor.

API key fields show only saved-key placeholders or empty inputs. No credentials are included.
