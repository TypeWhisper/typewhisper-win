<UserControl x:Class="TypeWhisper.Windows.Controls.Overlay.CompactBadgeIndicatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:overlay="clr-namespace:TypeWhisper.Windows.Controls.Overlay"
             xmlns:vm="clr-namespace:TypeWhisper.Windows.ViewModels">

    <Border x:Name="BadgeBorder"
            CornerRadius="21"
            Background="#EE11161D"
            BorderBrush="#24FFFFFF"
            BorderThickness="1"
            HorizontalAlignment="Center"
            Width="42"
            Height="42"
            Padding="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="14" Opacity="0.32" ShadowDepth="3"/>
        </Border.Effect>

        <Grid>
            <overlay:WaveformWidget HorizontalAlignment="Center" VerticalAlignment="Center">
                <overlay:WaveformWidget.Style>
                    <Style TargetType="{x:Type overlay:WaveformWidget}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State}" Value="{x:Static vm:DictationState.Recording}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </overlay:WaveformWidget.Style>
            </overlay:WaveformWidget>

            <overlay:IndicatorWidget HorizontalAlignment="Center" VerticalAlignment="Center">
                <overlay:IndicatorWidget.Style>
                    <Style TargetType="{x:Type overlay:IndicatorWidget}">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State}" Value="{x:Static vm:DictationState.Recording}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </overlay:IndicatorWidget.Style>
            </overlay:IndicatorWidget>
        </Grid>
    </Border>
</UserControl>
