<UserControl x:Class="TypeWhisper.Windows.Controls.Overlay.HotkeyModeWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:TypeWhisper.Windows.Converters"
             xmlns:svc="clr-namespace:TypeWhisper.Windows.Services">
    <UserControl.Resources>
        <conv:HotkeyModeLabelConverter x:Key="HotkeyModeLabel"/>
    </UserControl.Resources>
    <Border BorderThickness="1" CornerRadius="7" Padding="6,2">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#16FFFFFF"/>
                <Setter Property="BorderBrush" Value="#1FFFFFFF"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding CurrentHotkeyMode}" Value="{x:Static svc:HotkeyMode.Toggle}">
                        <Setter Property="Background" Value="#24F5B84B"/>
                        <Setter Property="BorderBrush" Value="#40F5B84B"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <TextBlock Text="{Binding CurrentHotkeyMode, Converter={StaticResource HotkeyModeLabel}, FallbackValue='Hybrid', TargetNullValue='Hybrid'}"
                   FontSize="9" FontWeight="SemiBold" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="#BFFFFFFF"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentHotkeyMode}" Value="{x:Static svc:HotkeyMode.Toggle}">
                            <Setter Property="Foreground" Value="#F5B84B"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Border>
</UserControl>
